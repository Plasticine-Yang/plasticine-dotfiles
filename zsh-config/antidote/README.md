# Zsh Reference Configuration

This directory is retained as Reference Configuration for the Owner's shell
preferences. It is not an installation entrypoint.

The `plasticine` Workstation CLI is responsible for planning Zsh availability,
PATH launch entries, and any authorized login-shell System Change. Plugin
installation output and generated loader files are Tool-managed State rather
than Release inputs.
