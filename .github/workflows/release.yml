name: release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  validate:
    name: release validation gates
    runs-on: ubuntu-latest
    env:
      HOME: ${{ github.workspace }}/.plasticine-ci-home
      REQUIRE_SHELLCHECK: "1"
      PLASTICINE_VERSION: ${{ github.ref_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Prepare isolated HOME
        run: mkdir -p "$HOME"

      - name: Run release validation gates
        run: scripts/validate-release.sh

  publish:
    name: publish immutable assets
    runs-on: ubuntu-latest
    needs:
      - validate
      - native-smoke
    env:
      HOME: ${{ github.workspace }}/.plasticine-ci-home
      GH_TOKEN: ${{ github.token }}
      PLASTICINE_VERSION: ${{ github.ref_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Prepare isolated HOME
        run: mkdir -p "$HOME"

      - name: Refuse duplicate release
        run: |
          if gh release view "$PLASTICINE_VERSION" >/dev/null 2>&1; then
            echo "release already exists: $PLASTICINE_VERSION" >&2
            exit 1
          fi

      - name: Build release assets
        run: scripts/build-release.sh dist

      - name: Verify complete publication plan
        run: |
          go run ./cmd/plasticine-gate publication "$GITHUB_REF_NAME" "$PLASTICINE_VERSION" dist install.sh

      - name: Create draft release with complete assets
        run: |
          prerelease_flag=""
          case "$PLASTICINE_VERSION" in
            *-*) prerelease_flag="--prerelease" ;;
          esac
          gh release create "$PLASTICINE_VERSION" \
            dist/plasticine_darwin_amd64 \
            dist/plasticine_darwin_arm64 \
            dist/plasticine_linux_amd64 \
            dist/plasticine_linux_arm64 \
            dist/checksums.txt \
            install.sh \
            --draft \
            --verify-tag \
            $prerelease_flag \
            --title "$PLASTICINE_VERSION" \
            --notes "Plasticine Workstation CLI $PLASTICINE_VERSION"

      - name: Publish completed release
        run: gh release edit "$PLASTICINE_VERSION" --draft=false

  native-smoke:
    name: release native smoke (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs:
      - validate
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
    env:
      HOME: ${{ github.workspace }}/.plasticine-ci-home
      PLASTICINE_VERSION: ${{ github.ref_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Prepare isolated HOME
        run: mkdir -p "$HOME"

      - name: Build release artifacts
        run: scripts/build-release.sh dist

      - name: Run native smoke
        run: |
          version="${PLASTICINE_VERSION:-$(git describe --tags --exact-match 2>/dev/null || printf '%s' dev)}"
          commit="$(git rev-parse HEAD)"
          commit_time="$(git show -s --format=%cI HEAD)"
          PLASTICINE_BINARY="dist/plasticine_$(go env GOOS)_$(go env GOARCH)" \
            PLASTICINE_EXPECT_VERSION="$version" \
            PLASTICINE_EXPECT_COMMIT="$commit" \
            PLASTICINE_EXPECT_COMMIT_TIME="$commit_time" \
            scripts/smoke.sh
