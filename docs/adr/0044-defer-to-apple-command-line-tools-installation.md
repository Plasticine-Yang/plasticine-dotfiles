# Defer to Apple's Command Line Tools installation

On macOS, Xcode or Apple's Command Line Tools satisfy Git and related development capabilities. If absent, an authorized Apply may launch `xcode-select --install`, then mark dependent Components as awaiting Owner action and require a later Apply after the system dialog completes; the CLI will neither install Homebrew for this purpose nor use unofficial silent-install workarounds.
