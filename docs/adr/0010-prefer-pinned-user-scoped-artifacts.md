# Prefer pinned user-scoped tool artifacts

Managed Tools with suitable official binaries will be installed from version-pinned, checksum-verified upstream artifacts into user-owned storage. Native package managers will be reserved for foundational or operating-system-bound dependencies that cannot reasonably follow that path, keeping most Reconciliation reproducible and unprivileged without taking on custom builds for system software.
