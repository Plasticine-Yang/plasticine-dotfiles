# Keep the initial command surface small

The first `plasticine` CLI will expose only `plan`, `apply`, `doctor`, and `version`. First use remains Apply, Plan already answers drift status, and upgrades rerun Bootstrap; component filtering, adoption, confirmation, and System Change authorization remain options on Plan or Apply rather than growing parallel command paths.
