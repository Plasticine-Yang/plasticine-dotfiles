# Bootstrap selects stable Releases

The fixed Bootstrap URL will select the latest stable Release by default and allow the caller to request an exact version for reproducibility or rollback. It will neither execute the `main` branch nor select a prerelease implicitly; after installing the chosen Workstation CLI, it will invoke that same version for the first Reconciliation.
